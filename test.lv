heyhey = 99

menu = {
    tacos: 10, burritos: 20
}

specials = ['twelve', 'thirteen', 'five']

example = function(arg,arg2) {
    hello = 10 * arg + arg
    return hello
}

result = example(10, 20)

val = 0

while val < 10
    val = val + 1
end while


worf = 0
for x in menu
    worf = worf + menu.x
end for

specials_txt = ''

for y in specials
    specials_txt = specials_txt + ' ' + y

menu['empanadas'] = menu.tacos + menu.burritos
